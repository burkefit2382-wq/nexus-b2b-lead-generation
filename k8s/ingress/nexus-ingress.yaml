apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nexus-ingress
  namespace: nexus
  labels:
    app.kubernetes.io/name: nexus-ingress
    app.kubernetes.io/part-of: nexus-lead-intelligence
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-production
    nginx.ingress.kubernetes.io/proxy-body-size: "10m"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - nexuscloud.sh
      secretName: nexuscloud-sh-tls
  rules:
    - host: nexuscloud.sh
      http:
        paths:
          - path: /hubspot
            pathType: Prefix
            backend:
              service:
                name: nexus-backend
                port:
                  number: 8000
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: nexus-backend
                port:
                  number: 8000
          - path: /
            pathType: Prefix
            backend:
              service:
                name: nexus-backend
                port:
                  number: 8000
    - http:
        paths:
          - path: /hubspot
            pathType: Prefix
            backend:
              service:
                name: nexus-backend
                port:
                  number: 8000
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: nexus-backend
                port:
                  number: 8000
          - path: /
            pathType: Prefix
            backend:
              service:
                name: nexus-backend
                port:
                  number: 8000
