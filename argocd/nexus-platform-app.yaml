apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: nexus-platform
  namespace: argocd
  labels:
    app.kubernetes.io/name: nexus-platform
    app.kubernetes.io/part-of: nexus-platform
spec:
  project: default
  source:
    repoURL: https://github.com/burkefit2382-wq/nexus-b2b-lead-generation.git
    targetRevision: main
    path: platform
  destination:
    server: https://kubernetes.default.svc
    namespace: nexus
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
